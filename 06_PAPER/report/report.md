# 变量溯源、控制律门控与数值健康记录：CE-QUAL-W2 官方示例的方法学审计

- 生成时间：2026-08-16 18:01:24
- 数字权威：`06_PAPER/analysis/*.json`
- 未重跑完整 W2；未 git commit/push

## 目录

1. [封面](#cover)
1. [摘要](#abstract)
1. [研究背景与目的](#background)
1. [数据与方法](#methods)
1. [研究过程](#process)
1. [结果展示](#results)
1. [分析与讨论](#discussion)
1. [主要结论](#conclusions)
1. [不足与展望](#limitations)
1. [附录](#appendix)


## 摘要

本报告汇编 CE-QUAL-W2（二维横向平均水动力—水质模型；Cole & Wells, 2003）官方示例复现与后续方法学审计产物，围绕四个相互独立、但必须联立声明的评估条件展开：（1）变量溯源记录 VPR（Variable Provenance Record，输出文件/列名/断面 segment I / 层 layer K / 单位 / 派生链 / 时间支撑 / 配对容差）；（2）控制律状态（以 Bonneville 总溶解气体 TDG，Total Dissolved Gas 的目标控制器 TDGTA 为例）；（3）数值健康记录 NHR（Numerical Health Record，负表面层厚度回退、exit 0 是否掩盖警告、DLTINTER 状态）；（4）证据类型声明（对观测技能 vs 输出通道内部一致性）。

在同一套 CCIW（Cascade Island / Bonneville 尾水水质站）观测上，Bonneville 三口径给出窄带 R²（决定系数，Coefficient of Determination）约 0.5082–0.5512，而 NSE（Nash–Sutcliffe Efficiency，纳什效率系数）却为 -2.8044 / 0.5 / -2.7516：R² 最高的 C 口径恰恰是 NSE 最差之一。技能最好、β≈1、模拟峰值钉在约 120.1% 的序列只存在于控制器门控文件 <code>TDGTarget_output.csv</code>；TDGTA=OFF 时该文件消失，但 SYSTDG 仍把控制前快照写到 <code>TDG_output.csv</code>（ON/OFF 逐日相同），因此不能写成「物理量被删除」。

命题已推广：DeGray 同一 TSR（Time Series，时间序列输出）文件中表层 T2 对库容均温 Tvolavg 给出 R²=0.9027 而 NSE=-0.5855（α=0.346，β=0.608）；Columbia 溶解氧 DO（Dissolved Oxygen）断面 I=49 vs I=33 的 R²=0.6505 而 NSE=-1.4821。<strong>二者皆无独立观测，属于输出通道内部一致性，不是对观测技能。</strong> Long Lake 在官方 DLTINTER=ON 下负厚度计数为 5/4/1/5（DLTMAX@JDAY30 = 20/50/100/200 s），非单调；DLTINTER=OFF 后全为 0——故 NHR 是报告项，不是「减小时间步更不稳」定律。

库内 CCIW 与 2026 年下载的 DART 小时值配对 n=17805，MAE=0.026537%，|Δ|≤0.051 匹配率=0.994945；未见示例观测被实质性改写的证据。2016–2025 有效小时超 120% 比例为 21.2%，但<strong>无样本外 NSE</strong>（模型约到 2011）。 Columbia 湿段 SOD（Sediment Oxygen Demand，底泥耗氧）均值 0.876 gO₂/m²/d，落在 Almeida 扫描带内比例 89.5%，但是 DeGray 模板移植，非现场率定。 W5 文献审计：38 篇中可重建 VPR 仅 2/38（5.3%），全文获取 9/38，其余标 unknown。

关键词：CE-QUAL-W2；变量溯源（VPR）；NSE / KGE / R²；总溶解气体（TDG）；TDGTA 控制器；数值健康记录（NHR）；内部一致性；条件化评估


### 术语与符号全量说明（首次出现）


*表 G　术语速查*
| 术语 / 符号 | 物理意义、方程溯源与为何引入 |
| --- | --- |
| CE-QUAL-W2 | 二维横向平均（laterally averaged）水动力—水质模型（Cole & Wells, 2003）。本报告复现官方 v4.5.5 可执行文件 w2_v455_ifx.exe 的示例，不发明新过程方程。 |
| JDAY | 模型儒略日（Julian day）。Bonneville 中 40544=2011-01-01（Excel 序列原点 1899-12-30）。配对窗与超标统计都必须声明 JDAY 范围。 |
| segment / I | 纵向河段编号。同一变量名在不同 I 上是不同空间支撑；Columbia DO 的核心教训是错站比错层更危险。 |
| layer / K / KT / KTMAX | 垂向层号 K；KT 为当前水面所在表层索引；KTMAX 为网格允许的最大层索引上界（活动层窗口随水位变化）。不写 K/KT，表/底层输出无法复原。 |
| H1(KT,I) / ZMIN | H1(KT,I) 为断面 I 当前表层厚度。若 H1&lt;0，几何非法，源码路径写警告并把时间步回退到 DLTMIN 后重算（见 NHR）。ZMIN 与表层水位相对层顶偏移相关，参与加/减层判定——层增减可以是合法响应，不等于 H1&lt;0 故障。 |
| TSR / PRF / SNP / WDO | 时间序列、剖面、快照场、取水/结构混合输出通道。同名物理量可经不同文件与派生链写出；VPR 必须落到文件+列。 |
| TDG | Total Dissolved Gas，总溶解气体饱和度（%）。名字叫 TDG 不够，必须声明是亨利换算尾水、库内 TSR、SYSTDG 快照，还是 TDGTA 门控文件。 |
| SYSTDG / TDGTA | SYSTDG：控制前快照可写到 TDG_output.csv（口径 S）。TDGTA：目标控制器；后控制序列在 TDGTarget_output.csv（口径 B）。OFF 时 B 消失但 S 仍可写——禁止「物理量被删除」。 |
| CCIW / DART | CCIW：Cascade Island / Bonneville 尾水观测（技能对照）。DART：公开小时库，核对示例观测并提供多年超标频率（不是样本外 NSE）。 |
| R²（决定系数） | Coefficient of Determination。对仿射变换 s′=a·s+b 不敏感；α/β 偏离 1 时仍可能好看。本报告不当唯一技能标尺。 |
| NSE（Nash–Sutcliffe Efficiency） | NSE = 1 − Σ(s−o)² / Σ(o−ō)²（Nash & Sutcliffe, 1970）。NSE&lt;0 劣于均值预报。内部一致性也会算 NSE，但必须标 internal_consistency。 |
| KGE / r / α / β | Kling–Gupta Efficiency（Gupta et al., 2009）：KGE = 1 − √[(r−1)²+(α−1)²+(β−1)²]。r=corr；α=σ_s/σ_o；β=μ_s/μ_o。误指认与封顶常先坏在 α/β。 |
| PBIAS / MAE / RMSE | 百分偏差 / 平均绝对误差 / 均方根误差。辅助解读，不能单独替代 NSE/KGE，也不能拯救缺失的 VPR。 |
| VPR | Variable Provenance Record：文件、列、segment I、layer K、单位、派生链、时间支撑、配对容差。缺少 VPR 时跨研究并置拟合优度一般不能从指标本身建立可比性。 |
| NHR | Numerical Health Record：负厚度回退、exit 0 是否掩盖警告、DLTINTER、层增减等。主张随技能一并报告（should），不是普遍时间步定律。 |
| DLT / DLTMAX / DLTMIN / DLTINTER | 时间步长及其日程上限、下限、结点间是否线性插值。Long Lake：ON 时 5/4/1/5（20/50/100/200 s）非单调；OFF 全 0。不得写成「减小时间步更不稳」。 |
| SOD / CSOD / NSOD | Sediment Oxygen Demand 底泥耗氧及其碳源/氮源分量（gO₂ m⁻² d⁻¹）。Columbia 为移植参数量级检查，不是现场率定。 |
| forrtl | Intel Fortran 运行时库消息前缀。即便 exit 0，forrtl / w2.wrn 仍可能记录过严重几何警告；NHR 要求把这些警告从「正常结束」叙事中拆出来。 |
| NV / NIT | 时间步 violation 累计 / 积分步数等；NV≠负厚度次数。不要混成单一不健康分数。 |

上述术语不是互相独立的名词表，而是一条审计链：segment 与 layer 决定空间取样算子；JDAY 与配对容差决定时间取样算子；VPR 把这两个算子连到具体文件和列；R²、NSE、KGE、PBIAS、MAE、RMSE 只在这条链锁定后才有明确数学对象；TDGTA、SYSTDG、DLTINTER 等开关又决定该对象是否经过控制或数值路径变换；NHR 最后记录运行虽然结束、但中途是否发生几何回退。因此，本报告第一次出现缩写时不仅给全称，也说明它位于“物理过程—输出通道—评估统计”哪一层。


## 研究背景与目的

水库与河口水动力—水质模型的「率定好看」并不自动等于「评估对象可比」。当论文只报告 R²，而不声明输出文件、断面、层、控制律与数值警告时，读者无法判断两个 R² 是否在比较同一数学对象。Benicio 等（2024）对 38 篇 CE-QUAL-W2 富营养化应用给出综述表 2 的 R² 轴（0.32–0.977），这恰好提供了一个可审计的文献对照面：若多数研究缺少 VPR，则跨研究比较这些 R² 在方法上一般不能成立。

本项目的目的不是重新发明 CE-QUAL-W2，而是在官方示例上把四类评估条件写清楚，并给出可复算入口：

- 创新点 1：R² 对「相关但尺度错了」的通道盲；NSE/KGE（尤其 α、β）会暴露。
- 创新点 2：最优 TDG 序列由 TDGTA 门控；关控制器后评估文件消失≠物理量删除。
- 创新点 3：NHR 应随技能一并报告；5/4/1/5 仅 DLTINTER=ON 的诊断，OFF 全 0。
- 创新点 4 / W7：可复现性与 SOD 量级检查；Columbia SOD 为移植参数。
- W4/W5/W6：DART 核对与超标频率、文献 VPR 审计、w2eval run-card 协议。

> **科学边界（贯穿全文）**  
> DeGray / Columbia 无独立观测 → 内部一致性；无样本外 NSE；SOD 非现场率定；W5 全文 9/38，其余 unknown；不要把 2016–2025 的 21.2% 超标写成预报技能。


## 数据与方法


### 数据来源

- 指标 JSON：`06_PAPER/analysis/*.json`（本报告运行时直接读取）。
- 图：`06_PAPER/figures/*.png`（SciencePlots 重绘，dpi≥300）。
- 笔记：`06_PAPER/notes/W*_findings.md`、`STATUS_20260815.md`。
- run-card：`06_PAPER/w2eval/cards/`（VPR + 指标 + NHR，不自动跑模型）。
- 复现运行目录：`05_REPRO_RUNS/run_20260811_fixed`、`run_20260814_*`、`run_20260815_*`。


### 评估指标定义

设观测（或对照序列）为 o_t、模拟（或另一通道）为 s_t，样本量为 n。本报告所有「对观测」数字均来自 Bonneville–CCIW 配对；DeGray/Columbia 的 ref 是另一输出通道，不是野外观测。公式以纯文本给出（报告为自包含 HTML，不加载 MathJax CDN）。

- R²：线性相关结构的决定系数。对 s′ = a·s + b 一类仿射变换不敏感，因此当 α、β 严重偏离 1 时仍可能「看起来不错」。
- NSE = 1 − Σ(s−o)² / Σ(o−ō)²。NSE&lt;0 表示比用观测均值预报更差。它惩罚幅度与偏差，是技能的主标尺之一。
- KGE = 1 − √[(r−1)² + (α−1)² + (β−1)²]，其中 α = σ_s/σ_o，β = μ_s/μ_o。变量误指认常通过 α（方差被平均压掉或被另一站放大）和 β（均值错位）暴露。
- PBIAS / MAE：总体偏高偏低与平均绝对误差；辅助解读，不单独替代 NSE/KGE。


### 指标的物理解释与互补关系

R² 在本报告中主要回答“两个序列是否沿近似线性方向共同变化”，并不回答“数值是否相等”。若 s=a·o+b 且噪声很小，即使 a 远离 1 或 b 很大，R² 仍可接近 1；这正是为什么 DeGray 表层温度与库容均温能同步随季节升降，却不能互换。NSE 把逐时平方误差与观测围绕均值的总变差相比：分子是模型或替代通道留下的误差能量，分母是“什么都不做、永远报观测均值”的基准误差能量。NSE<0 的物理含义不是“没有相关”，而是该序列作为逐时数值替代品还不如均值基准。

KGE 将误差来源分解为三条相互正交的诊断轴：r 控制时序共变，α=σs/σo 控制波动幅度，β=μs/μo 控制平均水平。KGE 的欧氏距离形式意味着任何一项显著偏离 1 都会拉低总分。PBIAS=100·Σ(s−o)/Σo 只看带符号总偏差，正负误差可抵消；MAE=n⁻¹Σ|s−o|保留平均绝对距离；RMSE=√[n⁻¹Σ(s−o)²]对大误差赋予更高权重。因而，PBIAS 近零不保证逐时准确，MAE 小不说明峰值尾部可达，RMSE 大则可能由少量极端误差主导。报告必须联读，而不能挑最漂亮的一项。

这里的 o_t 在 Bonneville–CCIW 段是观测；在 DeGray 与 Columbia 段只是被指定为 ref 的另一输出通道。公式完全相同不代表证据类型相同：同一个 NSE 数值，在前者可称观测技能，在后者只能称内部一致性。这一区分是语义和研究设计边界，不是计算软件自动识别的属性。


### 变量溯源记录（VPR）的最小闭环

VPR 的八个字段共同定义一个可复算序列：文件确定输出通道，列确定变量标签，segment I 确定纵向位置，layer K/KT 或空间算子确定垂向支撑，单位防止尺度混淆，派生链说明是否经过亨利换算或控制器，时间支撑区分瞬时、小时、日均与快照，配对容差说明如何把两条不同时标的序列组成样本。少一个字段，复算者都可能抽到另一条“同名”曲线。VPR 不是元数据装饰，而是评价函数 f(s,o) 中 s 与 o 的定义域。


### 控制律与数值路径的条件化设计

TDGTA ON/OFF 是控制律状态对照，不是删除水质过程的消融实验。ON 时控制器读取控制前 SYSTDG 快照，按照目标带重新分配泄流，并写出 TDGTarget_output；OFF 时目标文件不生成，但 TDG_output、TSR 与亨利派生链仍可存在。因此实验因子是“是否启用目标控制与条件输出”，不是“是否计算 TDG”。同理，DLTINTER ON/OFF 控制 DLTMAX 日程结点之间是否插值；它改变实际时间步上限轨迹，而不等同于把整个 JDAY30–40 窗固定为某个硬上限。


### 数值健康记录（NHR）的判据

NHR 将终止状态与中途路径分开。completed/exit 0 只说明程序到达终点并正常退出；H1(KT,I)<0 说明某断面的当前表层厚度成为负值，源码路径随即把 CURMAX 回退到 DLTMIN 并重算。ZMIN 参与水面相对层界的几何判定；Add layer/Subtract layer 是水位跨层界时调整活动层窗口的事件，本身可完全合法。故 NHR 分项保留负厚度、DLTMIN 回退、层增减、violation 与终止状态，不把它们压缩成未经验证的单一“健康分”。


### 配对与容差

较短序列驱动最近邻配对；缺失值阈约 −90。Bonneville A/C 口径容差 0.05 d，B/S 日尺度容差 0.6 d（与既有评估脚本一致）。CCIW 有效 TDG 并不覆盖全年：n=1614 落在 JDAY 40613.58–40681.54（约 2011-03-11 至 2011-05-18）。


### 文献审计方法（W5）

编码对象为 Benicio 等（2024）表 1 的 38 篇。VPR 可重建（yes）要求同时给出可定位断面、层/深度、对照变量与时段。全文可读 9/38；付费墙且摘要不够者标 unknown，不编造。


## 研究过程

工作按工作包推进，本报告不重跑模型，只消费已落盘产物：


*表 P　研究过程一览*
| 工作包 | 内容 |
| --- | --- |
| W1 | DeGray T / Columbia DO 多通道内部一致性；推广 R²–NSE 分离 |
| W2 | NHR 解析 + Long Lake DLTMAX×DLTINTER 扫描 |
| W3 | Bonneville TDGTA ON/OFF 溯源一致对照 |
| W4 | DART CCIW 下载、库内核对、超标频率、泄流再分配 |
| W5 | 38 篇 VPR/指标报告率审计 |
| W6 | w2eval run-card MVP（五张卡） |
| W7 | Columbia SOD vs Almeida 扫描带量级检查 |
| 图 | SciencePlots 重绘 `06_PAPER/figures/`（见 SCIENCEPLOTS_REDRAW_LOG.md） |

数字审计（2026-08-16）：草稿关键数字相对 JSON 检查 40 项，PASS=40，FAIL=0（`notes/P1_number_audit_20260816.md`）。


### 证据分层与禁止项（过程纪律）

全过程把证据分成四层，并在写数字前先声明层别：（L1）对观测技能——仅 Bonneville CCIW 配对窗；（L2）输出通道内部一致性——DeGray T、Columbia DO；（L3）观测–观测核对与描述统计——DART 匹配、超标频率；（L4）数值健康与参数移植量级——NHR、SOD。跨层复用同一指标名（例如 NSE）而不改下标，是本项目明确禁止的写法。

禁止项在过程笔记中反复核对：不得编造缺失 JSON 字段；不得把 OFF 缺失的 B 文件写成零序列；不得把 2016–2025 超标比例写成样本外 NSE；不得把 W5 unknown 改写成 no；不得在未获独立观测前给 DeGray/Columbia 写 skill。本报告生成脚本从 analysis/*.json 读数，缺失统一输出「待补充」。

图件优先使用 `06_PAPER/figures/*.png`（SciencePlots；中文回退 Microsoft YaHei）。报告 HTML 为单文件自包含：CSS 内嵌、图片 Base64、无 CDN、无相对/网络图路径。深度图解由 `report_fig_narratives.py` + `report_fig_narratives_extra.py` 组装，对每张纳入图执行九段式讲解，子图逐一覆盖。


## 结果展示


### 4.1 Bonneville TDG：三口径技能与 TDGTA 门控（W3）

为什么做：若评估只写「TDG R²=0.5x」，读者不知道用的是亨利换算尾水、控制器输出、库内 TSR，还是 SYSTDG 控制前快照。怎么做：同一 CCIW、n=1614，固定配对窗，切换 TDGTA ON/OFF 并逐文件对照。


*表 1　Bonneville TDG 对 CCIW（n=1614）三口径 + SYSTDG 快照*
| 运行 | 口径 | 文件 | R² | NSE | KGE | α | β | sim max |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ON | A | c_wdo_76.csv + t_wdo_76.csv | 0.5082 | -2.8044 | 0.4089 | 1.513 | 0.941 | 121.3 |
| ON | B | TDGTarget_output.csv | 0.5332 | 0.5 | 0.7152 | 0.909 | 0.999 | 120.1 |
| ON | C | BON_tsr_1_seg40.csv | 0.5512 | -2.7516 | 0.3854 | 1.555 | 0.941 | 123.4 |
| ON | S | TDG_output.csv | 0.5614 | 0.3573 | 0.7057 | 1.154 | 1.007 | 127.5 |
| OFF | A | c_wdo_76.csv + t_wdo_76.csv | 0.5212 | -2.3371 | 0.1603 | 1.791 | 0.953 | 124 |
| OFF | B | TDGTarget_output.csv | — | 文件不存在 | — | — | — | — |
| OFF | C | BON_tsr_1_seg40.csv | 0.5511 | -2.7522 | 0.3855 | 1.555 | 0.941 | 123.4 |
| OFF | S | TDG_output.csv | 0.5614 | 0.3573 | 0.7057 | 1.154 | 1.007 | 127.5 |

观测 max=129.1%；251/1614（15.6%）点 &gt;120%。 ON B 配对峰值 120.1% 钉在目标上限；OFF 时 A/S 可超过 120%，但配对时刻仍未稳定复现观测 129.1%。


### 图 1　TDGTA ON/OFF 多口径时间序列（全年与 CCIW 窗）

![图 1　TDGTA ON/OFF 多口径时间序列（全年与 CCIW 窗）](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/W3_tdgta_on_off_timeseries.png)

**背景与作用：** 本图在全篇中承担 Bonneville TDG 条件化对照的「时间形态入口」：它先让读者看见控制器开启前后各输出通道如何随 JDAY（Julian day / 模型日）演化；再把技能配对窗（与 n=1614 对应）从全年曲线中显式标出，防止把窗外形态偷算进技能；全篇作用是为后续散点、KGE 分解与可达范围图提供「条件何时存在」的共同时间坐标系；权威配对在 JSON 中固定为 CCIW 观测与多口径模拟，n=1614，窗与容差见 w3_tdgta_off_metrics.json。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 读图顺序建议为：先读上面板全年，识别控制介入后蓝线（ON-B）是否出现目标带平顶；再读灰带位置，确认哪些时段才进入技能样本；然后进入下面板放大窗，逐条对比灰点观测与蓝/绿/橙等模拟通道；最后才回头解释指标：窗外再漂亮也不能写入 NSE/KGE；纵轴为 TDG%，横轴为 JDAY；缺失的 OFF-B 不是画成 0，而是无文件。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** 曲线/点的对象定义必须按口径分开：灰点=CCIW 有效观测（尾水站）；A=尾水 N2+DO 亨利派生链（c_wdo/t_wdo）；B=TDGTA 控制后写入 TDGTarget_output 的目标跟踪序列；C=库内 TSR seg40 TDG 通道；S=SYSTDG 在控制再分配前写入 TDG_output 的快照；ON-B 权威峰值约 120.09%，观测峰值 129.1%。

**逐子图 / 逐面板精读：** 逐面板：上面板覆盖约 JDAY40544–40909 的模型时段，灰色竖带只标技能窗，窗外曲线只提供条件上下文；下面板放大有效窗：观测可先低后高并越过 120%；ON-B 受目标约束呈平顶/窄带跳变；S 保留更宽峰谷且 ON≡OFF；A/C 在窗前段可系统性偏低；两板合读的唯一合法结论结构是：条件存在于何时 + 窗内对象如何偏离，而不是「全年看起来挺像」。

**物理意义与方程链：** NSE（Nash–Sutcliffe Efficiency，纳什–萨特克利夫效率）定义为 NSE=1−Σ(sim_i−obs_i)²/Σ(obs_i−obs̄)²：分子是模拟相对观测的平方误差总和，分母是观测相对其均值的变程；NSE=1 表示逐点重合，NSE=0 表示不比用观测均值作预报更好，NSE<0 表示比均值预报更差。它被引入，是因为只报相关或 R² 时无法惩罚整体平移、缩放与截顶。R²（Coefficient of Determination，决定系数）度量线性相关强度的平方，对整体平移与均匀缩放不敏感，因此可以在偏差很大时仍然偏高；它适合描述同相程度，不适合单独承担「技能」声明。亨利定律链路把分压/浓度换算到 TDG% 表达，因此 A 不是「另一种画法」而是另一条派生链；B 的平顶来自控制目标与泄流再分配后的值域收缩，不是扩散方程把高值磨平。

**能得出什么结论：** 可成立的结论：在同一 CCIW 上，ON 三口径 R² 约为 0.5082/0.5332/0.5512，而 NSE 约为 -2.8044/0.5/-2.7516；技能最好且均值接近的是门控 B，不是「物理量被删掉」；S 在 OFF 仍存在且与 ON 逐日相同，证明过程输出与门控文件必须分列。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 证据边界：不支持无条件预报外推；不支持把窗外曲线计入 n=1614；不支持仅凭曲线形状分离参数误差与控制动作；无样本外 NSE（JSON computed 语境下未给出 OOS）。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** 排除误读：蓝线贴近≠纯物理率定成功；OFF-B 空缺≠零值序列；S 的正 NSE≠OFF-B；120% 线≠1:1 理想线。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：这张图像「开没开巡航定速」的行车记录。定速打开时，车速表（B）会被按在目标附近；关掉定速，车速表文件可以消失，但发动机转速记录（S）还在。考试分数只应在划定的考试时段（灰带）里算，不能把整年路况都算进去。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 图 2　TDGTA ON/OFF 多口径 1:1 散点

![图 2　TDGTA ON/OFF 多口径 1:1 散点](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/W3_tdgta_on_off_scatter.png)

**背景与作用：** 本图把时序对照压缩为 1:1 散点四宫格，用来暴露「相关尚可但结构错位」的形态：水平条带、截顶、多团簇。全篇作用是把 R²–NSE 分离从口号落实为可指认的点云几何，并强制读者在同一观测轴上比较不同文件对象。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 每个子图横轴观测、纵轴模拟；黑虚线是数值相等，红线只标 120% 阈值。先看点云是否贴 1:1，再看是否被水平截顶，再看是否多团簇，最后读角标指标。四图必须按口径与 ON/OFF 标签分读，禁止脑内平均成「一个 TDG」。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** ON-B：R²=0.5332，NSE=0.5，β≈0.9986，sim_max≈120.09，呈控制截顶；ON-A：R²=0.5082，NSE=-2.8044，α≈1.5133；ON-C：R²=0.5512，NSE=-2.7516（R² 最高之一却 NSE 深负）；ON-S：R²=0.5614，NSE=0.3573，为控制前快照而非 B。OFF-B 文件不存在，对应面板为空/不可用。

**逐子图 / 逐面板精读：** 左上 ON-B：近水平带与 y≈120% 平顶，说明高观测被映射到相近目标；右上 OFF-A：低值团+过渡带+主团，显示非单一均匀误差；左下 OFF-S：日尺度条带，均值可接近但对象仍是快照；右下 OFF-C：与库内通道空间支撑不一致，NSE 深负。合读重点：对象不同，指标不可直接当「同一变量的四种画法」。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** NSE（Nash–Sutcliffe Efficiency，纳什–萨特克利夫效率）定义为 NSE=1−Σ(sim_i−obs_i)²/Σ(obs_i−obs̄)²：分子是模拟相对观测的平方误差总和，分母是观测相对其均值的变程；NSE=1 表示逐点重合，NSE=0 表示不比用观测均值作预报更好，NSE<0 表示比均值预报更差。它被引入，是因为只报相关或 R² 时无法惩罚整体平移、缩放与截顶。R²（Coefficient of Determination，决定系数）度量线性相关强度的平方，对整体平移与均匀缩放不敏感，因此可以在偏差很大时仍然偏高；它适合描述同相程度，不适合单独承担「技能」声明。α（方差比）回答模拟波动是否被压扁或放大；β（均值比）回答平均水平是否整体偏高/偏低。二者进入 KGE 的欧氏惩罚，并与 NSE 对偏差敏感的直觉一致，但提供更可解释的指纹。水平条带提示时间支撑（日尺度输出对小时观测）差异。

**能得出什么结论：** 结论：同一观测上，口径选择可翻转「谁最好」；B 的好处绑定 control_state=ON 与门控文件；C 的高 R² 不能拯救其负 NSE。这正是 VPR 必须写入报告的原因。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 不能分解参数/站位/控制各自贡献到唯一百分比；无 OOS；OFF-B 不可补零。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** 红线不是理想线；S≠B；截顶≠复现 129.1% 峰值；总 R² 不能抹掉多团簇。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：四张答题卡考的是四道题。有的题被老师改分规则（控制器）截了顶，分数好看；有的题根本不在同一个考场（库内 vs 尾水）。不能把四张卡平均成「数学考得如何」。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 图 3　KGE 分量分解（r / α / β）

![图 3　KGE 分量分解（r / α / β）](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/W3_tdgta_kge_decomposition.png)

**背景与作用：** 本图把综合技能拆成 r/α/β，回答「分数被谁拖累」。全篇作用是连接散点形态与公式惩罚，使读者看到：β≈1 仍可能因 α 偏离而损失 KGE，并与 ON-B 的 KGE=0.7152 等权威数字对齐。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 每个横轴组是一个逻辑子图（某运行×口径）。虚线 1 是三项理想值。先找缺席（OFF/B），再比较橙柱 α 与绿柱 β 谁离 1 更远，最后才看综合 KGE。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** ON/A：r≈0.7129，α≈1.5133，β≈0.9411；ON/B：r≈0.7302，α≈0.9087，β≈0.9986；ON/C：r≈0.7424，α≈1.5549，β≈0.9414；ON/S：r≈0.7492，α≈1.1539，β≈1.0072。OFF/B 无柱。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 按组逐读：ON/B 均值项最好但波动被压缩；ON/A 与 ON/C 方差项偏大；OFF/A 的 α 进一步升高；OFF/S 与 ON/S 一致，呼应快照不被控制器改写。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** KGE（Kling–Gupta Efficiency）=1−√[(r−1)²+(α−1)²+(β−1)²]，其中 r 为相关系数，α=σ_sim/σ_obs 为方差比，β=μ_sim/μ_obs 为均值比；三项理想值均为 1。KGE 被引入以把「同相、同幅度、同水平」拆开诊断，避免单一综合分掩盖来源。α（方差比）回答模拟波动是否被压扁或放大；β（均值比）回答平均水平是否整体偏高/偏低。二者进入 KGE 的欧氏惩罚，并与 NSE 对偏差敏感的直觉一致，但提供更可解释的指纹。

**能得出什么结论：** 分解支持「B 的好处来自均值对齐与波动压缩（控制后果）」这一条件叙事，而不是「找到了正确气体交换参数」的无条件叙事。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 分解≠因果鉴定；缺席≠零；不可把 KGE 排名当参数优化目标而不固定 VPR。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** α>1 不是越好；β≈1 不是逐时准；OFF/B 不补零。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：像体检单把「总分」拆成血压、血糖、血脂。有人总分高是因为某一项被仪器量程卡住（控制截顶），不是三项都健康。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 图 4　观测可达范围与控制器 120% 封顶

![图 4　观测可达范围与控制器 120% 封顶](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/fig05_tdg_reachable_range.png)

**背景与作用：** 可达范围图比较配对窗内观测与各口径模拟的极值，回答「能不能到」而非「对不对」。权威：观测 max=129.1，n_obs_gt_120=251/1614；ON-B sim_max≈120.09。全篇用于支撑「门控收缩值域」与「超标频率≠技能」。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 先看观测柱/点是否越过 120%，再看各口径 sim 极值是否被钉住，最后对照 JSON 的 frac_obs_gt_120 与 frac_sim_gt_120。120% 是管理阈值线，不是物理溶解度上限。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** 观测极值、A/B/C/S 的 sim 极值是不同对象；B 的平顶是控制后果；S/A/C 可更高但不自动更准。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 单面板但按口径分组读：每个口径是一个逻辑子图。重点对比 B 与观测在 >120% 区间的不对称。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** NSE（Nash–Sutcliffe Efficiency，纳什–萨特克利夫效率）定义为 NSE=1−Σ(sim_i−obs_i)²/Σ(obs_i−obs̄)²：分子是模拟相对观测的平方误差总和，分母是观测相对其均值的变程；NSE=1 表示逐点重合，NSE=0 表示不比用观测均值作预报更好，NSE<0 表示比均值预报更差。它被引入，是因为只报相关或 R² 时无法惩罚整体平移、缩放与截顶。极值统计不进入 NSE 分母的替代定义；技能仍以配对序列为准。

**能得出什么结论：** 门控使 B 难以复现观测高端；可达范围讨论可以引用 2016–2025 超标频率作动机，但 JSON 明确 computed_nse 相关为假/未算：2016–2025 超标比例 21.2% 不是 OOS NSE。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 无 OOS；不证明 OFF 删除物理过程；raw max 与 paired max 必须分列。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** 不要把冲得高写成技能好；不要把 120% 当物理上限；不要把频率当预报。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：限速开着时，车速表很难显示你实际能飙到的最高速；路上超速的车多不多，也不是你这趟限速考试的分数。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 4.2 DeGray 水温与 Columbia DO：内部一致性推广（W1）

> **不是对观测技能**  
> 已检索官方示例目录：DeGray / Columbia 无独立水温或 DO 观测。下列 NSE/KGE 度量的是同一物理量在不同输出通道上的数值分歧（provenance disagreement）。


*表 2　DeGray 水温内部一致性（ref 不是观测）*
| 对照 id | n | R² | NSE | KGE | α | β |
| --- | --- | --- | --- | --- | --- | --- |
| DG_T2_vs_Tvolavg | 2943 | 0.9027 | -0.5855 | 0.2354 | 0.346 | 0.608 |
| DG_T2_vs_WDO | 2943 | 0.5293 | -0.3653 | 0.279 | 0.42 | 0.669 |
| DG_STR115_vs_GATE120 | 2943 | 0.5336 | -6.5825 | -0.4858 | 2.377 | 1.488 |
| DG_T2_vs_GATE120 | 2943 | 0.9995 | 0.9993 | 0.9943 | 0.998 | 0.995 |
| DG_T2_vs_PRF26bot | 296 | 0.0572 | -2.9461 | -0.3717 | 0.057 | 0.357 |
| DG_SNP31_sfc_vs_bot | 47 | 0.1969 | -2.0698 | -0.2558 | 0.065 | 0.373 |


*表 3　Columbia DO 内部一致性（ref 不是观测）*
| 对照 id | n | R² | NSE | KGE | α | β |
| --- | --- | --- | --- | --- | --- | --- |
| COL_DO_I45_vs_I49 | 116 | 0.2071 | -4.494 | 0.1665 | 0.682 | 1.544 |
| COL_DO_I45_vs_I33 | 116 | 0.3275 | -2.2675 | 0.3794 | 1.258 | 1.368 |
| COL_DO_I49_vs_I33 | 116 | 0.6505 | -1.4821 | 0.1243 | 1.846 | 0.886 |
| COL_DO_SNP45_sfc_vs_bot | 24 | 0.9321 | 0.9072 | 0.9362 | 0.971 | 0.955 |
| COL_DO_I45_ON_vs_OFF | 116 | 0.9721 | 0.9239 | 0.8367 | 1.159 | 1.034 |


### 图 5　DeGray 多通道水温时间序列

![图 5　DeGray 多通道水温时间序列](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/w1_degray_T_timeseries.png)

**背景与作用：** DeGray 水温多通道时序是内部一致性推广的第一现场：官方案例无独立 T 观测。全篇作用：证明「看起来一起涨落」不能写成 skill。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 先对齐季节相位，再看幅度与基线，最后才谈指标。任何通道都不得改称为观测。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** T2=表层，Tvolavg=库容均温，其它结构/闸门通道对应不同取水高程；关键对 T2 vs Tvolavg：R²=0.9027，NSE=-0.5855，α=0.3456，β=0.6077。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 若多面板，按通道逐条读；若单面板多曲线，把每条曲线当作子图阅读其物理取样算子。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** NSE（Nash–Sutcliffe Efficiency，纳什–萨特克利夫效率）定义为 NSE=1−Σ(sim_i−obs_i)²/Σ(obs_i−obs̄)²：分子是模拟相对观测的平方误差总和，分母是观测相对其均值的变程；NSE=1 表示逐点重合，NSE=0 表示不比用观测均值作预报更好，NSE<0 表示比均值预报更差。它被引入，是因为只报相关或 R² 时无法惩罚整体平移、缩放与截顶。R²（Coefficient of Determination，决定系数）度量线性相关强度的平方，对整体平移与均匀缩放不敏感，因此可以在偏差很大时仍然偏高；它适合描述同相程度，不适合单独承担「技能」声明。α（方差比）回答模拟波动是否被压扁或放大；β（均值比）回答平均水平是否整体偏高/偏低。二者进入 KGE 的欧氏惩罚，并与 NSE 对偏差敏感的直觉一致，但提供更可解释的指纹。单位同为 ℃ 不意味着状态投影相同。

**能得出什么结论：** 高相关可与负 NSE 并存；证据类型=internal_consistency。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 无观测技能；无 OOS；不可写入 skill 表。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** 贴近≠率定成功；季节同步≠NSE 为正。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：两支温度计都在夏天升高，但一支在水面、一支在「整壶水平均」，分数不能当天气预报。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 图 6　DeGray 水温通道 1:1 散点

![图 6　DeGray 水温通道 1:1 散点](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/w1_degray_T_scatter.png)

**背景与作用：** 散点把内部对翻译成 1:1 几何，是 R²–NSE 分离的可视化钉子。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 看点云相对 1:1 的偏移与拉伸；数字以 JSON 为准。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** R²=0.9027，NSE=-0.5855，α=0.3456，β=0.6077。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 单面板：每个点是同时刻两列配对；密度高峰不是观测众数。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** NSE（Nash–Sutcliffe Efficiency，纳什–萨特克利夫效率）定义为 NSE=1−Σ(sim_i−obs_i)²/Σ(obs_i−obs̄)²：分子是模拟相对观测的平方误差总和，分母是观测相对其均值的变程；NSE=1 表示逐点重合，NSE=0 表示不比用观测均值作预报更好，NSE<0 表示比均值预报更差。它被引入，是因为只报相关或 R² 时无法惩罚整体平移、缩放与截顶。R²（Coefficient of Determination，决定系数）度量线性相关强度的平方，对整体平移与均匀缩放不敏感，因此可以在偏差很大时仍然偏高；它适合描述同相程度，不适合单独承担「技能」声明。α（方差比）回答模拟波动是否被压扁或放大；β（均值比）回答平均水平是否整体偏高/偏低。二者进入 KGE 的欧氏惩罚，并与 NSE 对偏差敏感的直觉一致，但提供更可解释的指纹。

**能得出什么结论：** 内部一致性可出现高 R² 负 NSE；必须标 evidence_type。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 无独立实测则任何正 R² 不升级为现场验证。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** 禁止写「水温技能 R²=0.90」。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：两列数字「跟得紧」但一列更平、均值也不同，相关好看、对错不好看。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 图 7　DeGray KGE 分量（r/α/β）

![图 7　DeGray KGE 分量（r/α/β）](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/w1_degray_T_kge_bars.png)

**背景与作用：** KGE 分解指出损失来自哪一项，服务「综合分不可单独报」。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 三柱对虚线 1；先读最远项。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** 与散点同一对：α≈0.3456，β≈0.6077。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 逻辑子图=三个分量。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** KGE（Kling–Gupta Efficiency）=1−√[(r−1)²+(α−1)²+(β−1)²]，其中 r 为相关系数，α=σ_sim/σ_obs 为方差比，β=μ_sim/μ_obs 为均值比；三项理想值均为 1。KGE 被引入以把「同相、同幅度、同水平」拆开诊断，避免单一综合分掩盖来源。补充：MAE/RMSE/PBIAS 与 NSE/KGE 互补——MAE/RMSE 给绝对误差尺度，PBIAS 给百分比偏差方向，NSE/KGE 给相对技巧与分解；报告应并列而非互相替代。

**能得出什么结论：** 低综合分首先是取样算子差异的统计后果。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 非观测验证。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** 不要为提高 KGE 而改参却不固定 VPR。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：同涨同落还行，幅度和平均对不上，总分就被拖累。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 图 8　DeGray R²–NSE 平面

![图 8　DeGray R²–NSE 平面](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/w1_degray_T_r2_vs_nse.png)

**背景与作用：** 把该内部门对放到 R²–NSE 平面，形成与文献图可比的方法钉子（但证据层不同）。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 横轴 R² 纵轴 NSE；确认落在高 R²–负 NSE 区。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** 点坐标对应 R²=0.9027，NSE=-0.5855。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 单点逻辑子图：只表达这一对内部通道。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** NSE（Nash–Sutcliffe Efficiency，纳什–萨特克利夫效率）定义为 NSE=1−Σ(sim_i−obs_i)²/Σ(obs_i−obs̄)²：分子是模拟相对观测的平方误差总和，分母是观测相对其均值的变程；NSE=1 表示逐点重合，NSE=0 表示不比用观测均值作预报更好，NSE<0 表示比均值预报更差。它被引入，是因为只报相关或 R² 时无法惩罚整体平移、缩放与截顶。R²（Coefficient of Determination，决定系数）度量线性相关强度的平方，对整体平移与均匀缩放不敏感，因此可以在偏差很大时仍然偏高；它适合描述同相程度，不适合单独承担「技能」声明。

**能得出什么结论：** 分离是公式后果；协议应强制同报 NSE 与 VPR。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 不得进入观测技能表。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** 不要与 Bonneville 技能点混称。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：专门钉住「相关好≠误差小」。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 图 9　Columbia 多断面 DO 时间序列

![图 9　Columbia 多断面 DO 时间序列](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/w1_columbia_DO_timeseries.png)

**背景与作用：** Columbia DO 内部通道时序把口径教训推广到溶解氧；官方无独立 DO 观测。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 比较相位、幅度与基线；禁止称观测。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** 关键对 I49 vs I33：R²=0.6505，NSE=-1.4821。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 每条 I 通道当作子图：不同 segment/取水对应不同水团取样。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** NSE（Nash–Sutcliffe Efficiency，纳什–萨特克利夫效率）定义为 NSE=1−Σ(sim_i−obs_i)²/Σ(obs_i−obs̄)²：分子是模拟相对观测的平方误差总和，分母是观测相对其均值的变程；NSE=1 表示逐点重合，NSE=0 表示不比用观测均值作预报更好，NSE<0 表示比均值预报更差。它被引入，是因为只报相关或 R² 时无法惩罚整体平移、缩放与截顶。DO 受曝气、耗氧、分层与取水高程影响，指标不自动归因。

**能得出什么结论：** 内部一致性≠技能。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 无观测；无 OOS。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** 高 R² 对≠校准完成。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：河段不同取水口的氧「一起起伏」不等于「对河边测点负责」。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 图 10　Columbia DO 断面 1:1 散点

![图 10　Columbia DO 断面 1:1 散点](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/w1_columbia_DO_scatter.png)

**背景与作用：** DO 散点展示相关尚可、偏差巨大；推广 provenance ambiguity。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 1:1 偏离方向与拉伸；防多模态被总指标掩盖。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** R²=0.6505，NSE=-1.4821。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 单面板点云；必要时按季节/潮周期在脑中分子图。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** NSE（Nash–Sutcliffe Efficiency，纳什–萨特克利夫效率）定义为 NSE=1−Σ(sim_i−obs_i)²/Σ(obs_i−obs̄)²：分子是模拟相对观测的平方误差总和，分母是观测相对其均值的变程；NSE=1 表示逐点重合，NSE=0 表示不比用观测均值作预报更好，NSE<0 表示比均值预报更差。它被引入，是因为只报相关或 R² 时无法惩罚整体平移、缩放与截顶。R²（Coefficient of Determination，决定系数）度量线性相关强度的平方，对整体平移与均匀缩放不敏感，因此可以在偏差很大时仍然偏高；它适合描述同相程度，不适合单独承担「技能」声明。α（方差比）回答模拟波动是否被压扁或放大；β（均值比）回答平均水平是否整体偏高/偏低。二者进入 KGE 的欧氏惩罚，并与 NSE 对偏差敏感的直觉一致，但提供更可解释的指纹。

**能得出什么结论：** 通道不等价；先 VPR 后指标。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 非观测技能。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** 内部 NSE 不得进摘要当模型表现。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：趋势像，数值差一截，不能互相顶替。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 图 11　Columbia DO 的 KGE 分量

![图 11　Columbia DO 的 KGE 分量](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/w1_columbia_DO_kge_bars.png)

**背景与作用：** DO 的 KGE 分解与温度案例平行，演示方法可跨变量复用。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 读离 1 最远柱。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** 分量对应同一内部对的 r/α/β（以 JSON 为准）。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 三分量逻辑子图。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** KGE（Kling–Gupta Efficiency）=1−√[(r−1)²+(α−1)²+(β−1)²]，其中 r 为相关系数，α=σ_sim/σ_obs 为方差比，β=μ_sim/μ_obs 为均值比；三项理想值均为 1。KGE 被引入以把「同相、同幅度、同水平」拆开诊断，避免单一综合分掩盖来源。补充：MAE/RMSE/PBIAS 与 NSE/KGE 互补——MAE/RMSE 给绝对误差尺度，PBIAS 给百分比偏差方向，NSE/KGE 给相对技巧与分解；报告应并列而非互相替代。

**能得出什么结论：** 低分是取样差异统计后果，不是擅自改写的「模块失败」。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 内部诊断。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** 虚线 1 不是水质目标浓度。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：先写清比的是谁，再谈好不好。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 图 12　Columbia R²–NSE 平面

![图 12　Columbia R²–NSE 平面](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/w1_columbia_DO_r2_vs_nse.png)

**背景与作用：** DO 版本的 R²–NSE 钉子，强调跨案例重复而非运气。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 确认 evidence_type=internal。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** R²=0.6505，NSE=-1.4821。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 单点。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** NSE（Nash–Sutcliffe Efficiency，纳什–萨特克利夫效率）定义为 NSE=1−Σ(sim_i−obs_i)²/Σ(obs_i−obs̄)²：分子是模拟相对观测的平方误差总和，分母是观测相对其均值的变程；NSE=1 表示逐点重合，NSE=0 表示不比用观测均值作预报更好，NSE<0 表示比均值预报更差。它被引入，是因为只报相关或 R² 时无法惩罚整体平移、缩放与截顶。R²（Coefficient of Determination，决定系数）度量线性相关强度的平方，对整体平移与均匀缩放不敏感，因此可以在偏差很大时仍然偏高；它适合描述同相程度，不适合单独承担「技能」声明。

**能得出什么结论：** R² 不能单独承担技能声明。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 非技能点。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** 不要与观测技能点混叙事。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：又一次「好看相关、难看 NSE」。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 图 13　R²–NSE 综合图（案例 + 文献动机）

![图 13　R²–NSE 综合图（案例 + 文献动机）](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/fig04_r2_vs_nse_literature.png)

**背景与作用：** 文献对照图可视化 W5：缺 VPR 时跨研究 R² 几乎不可比。可重建 VPR 2/38；全文 9/38；unknown=19。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 区分 skill / internal / confirmed / unknown；缺 NSE 不补造。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** 表 2 确认 W2–观测技能 1/12；文献 KGE 报出数=0（不是得零分）。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 每类点是逻辑子图；禁止把 internal 点画进 skill 云。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** R²（Coefficient of Determination，决定系数）度量线性相关强度的平方，对整体平移与均匀缩放不敏感，因此可以在偏差很大时仍然偏高；它适合描述同相程度，不适合单独承担「技能」声明。NSE（Nash–Sutcliffe Efficiency，纳什–萨特克利夫效率）定义为 NSE=1−Σ(sim_i−obs_i)²/Σ(obs_i−obs̄)²：分子是模拟相对观测的平方误差总和，分母是观测相对其均值的变程；NSE=1 表示逐点重合，NSE=0 表示不比用观测均值作预报更好，NSE<0 表示比均值预报更差。它被引入，是因为只报相关或 R² 时无法惩罚整体平移、缩放与截顶。跨研究可比前提是评价自变量同类。

**能得出什么结论：** 数字好看≠对象清楚；报告规范动机成立。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** unknown 保持 unknown；无 OOS。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** KGE=0 不是大家都算过。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：没写清考场规则的分数，不能排行榜。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 4.3 DART 核对、超标频率与泄流再分配（W4）

库内 CCIW vs DART：n=17805，MAE=0.026537，匹配率(|Δ|≤0.051)=0.994945。结论：没有证据表明官方示例附带观测被实质性改过。

2011–2015 有效小时 &gt;120% = 14.6842%；2016–2025 = 21.2%。样本外 NSE：False —— 2016–2025 的 DART CCIW 小时 TDG/Spill 已下载并完成超标统计；**没有**计算样本外 NSE——模型尚未跑 2016 以后，不能假装有预报技能。

2011 泄流：QGT vs DART r≈0.868638；再分配日（R，116 天）DART 均泄≈173.9 kcfs，TDGTA≈39.2308 kcfs，r≈-0.596447。


### 图 14　库内 CCIW 与 DART 小时 TDG 散点

![图 14　库内 CCIW 与 DART 小时 TDG 散点](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/w4_cciw_vs_dart_scatter.png)

**背景与作用：** 观测–观测核对：示例 CCIW vs DART。n=17805，MAE=0.026537，匹配率=0.994945。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 贴 1:1 与极小离散；偏离先查缺测/舍入。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** 两轴都是观测 TDG%，无模拟。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 单面板。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** 这是数据 provenance 检验，不是水动力方程检验。补充：MAE/RMSE/PBIAS 与 NSE/KGE 互补——MAE/RMSE 给绝对误差尺度，PBIAS 给百分比偏差方向，NSE/KGE 给相对技巧与分解；报告应并列而非互相替代。

**能得出什么结论：** 支持示例观测未被实质性改写；不支持模型技能。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 无 OOS。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** MAE 不是模型误差。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：尺子没被偷换，但考试还没开始。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 图 15　库内 CCIW 与 DART 对照时间序列

![图 15　库内 CCIW 与 DART 对照时间序列](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/w4_cciw_vs_dart_timeseries.png)

**背景与作用：** 时序面核对观测底数；锚点 JDAY40544↔2011-01-01。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 两线重合则差异应在舍入/缺测；台阶才怀疑改文件。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** 本地示例 vs DART；不绘制模拟。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 可按年/季在阅读时切分子段。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** TDG% 同源核对，无亨利链。补充：MAE/RMSE/PBIAS 与 NSE/KGE 互补——MAE/RMSE 给绝对误差尺度，PBIAS 给百分比偏差方向，NSE/KGE 给相对技巧与分解；报告应并列而非互相替代。

**能得出什么结论：** 与散点一致支持高匹配。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 不延伸为模型验证。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** 观测一致≠模拟一致。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：两条实测曲线叠在一起。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 图 16　逐年有效小时超 120% 比例

![图 16　逐年有效小时超 120% 比例](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/w4_tdg_gt120_annual.png)

**背景与作用：** 分年超标频率属 L3 观测描述。2016–2025=21.2%；computed_nse=false。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 柱=有效小时中 >120% 比例；缺年不画零充数。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** 每年一根逻辑子图。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 按年逐柱读，并与模型时段约止 2011 对照。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** 120% 是管理阈值切割。补充：MAE/RMSE/PBIAS 与 NSE/KGE 互补——MAE/RMSE 给绝对误差尺度，PBIAS 给百分比偏差方向，NSE/KGE 给相对技巧与分解；报告应并列而非互相替代。

**能得出什么结论：** 频率可作动机，不可当 OOS NSE。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 无样本外技能。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** 禁止写模型预测 21.2%。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：后来超速小时变多，是路上的事实，不是考分。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 图 17　逐年 TDG 年最大

![图 17　逐年 TDG 年最大](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/w4_tdg_annual_max.png)

**背景与作用：** 年最大与频率互补：冲多高 vs 多久越线；同属观测描述。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 柱高=年最大 TDG%；缺年待补充。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** 极值聚合，不进 NSE。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 逐年子图；与 fig05 分清观测极值 vs 口径 sim_max。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** 极值对尖峰/缺测敏感。补充：MAE/RMSE/PBIAS 与 NSE/KGE 互补——MAE/RMSE 给绝对误差尺度，PBIAS 给百分比偏差方向，NSE/KGE 给相对技巧与分解；报告应并列而非互相替代。

**能得出什么结论：** 不能替代配对技能，不能填 OOS。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 非模型输出。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** 不要与 B 平顶直接比而不声明对象。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：有的年份冲特别高；高≠模型好。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 图 18　TDGTA 泄流再分配 vs DART 实测泄流

![图 18　TDGTA 泄流再分配 vs DART 实测泄流](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/w4_spill_tdgta_vs_dart.png)

**背景与作用：** 泄流对照说明 TDGTA 会改写流量路径，故 B 技能必须条件化。权威摘录：QGT vs DART r≈0.868638；再分配日实测约 173.8573 kcfs→约 39.2308 kcfs，r≈-0.596447。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 分清闸门/溢洪与 DART；先全期后看再分配子集。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** 流量序列，不是 TDG%；负相关子集对应控制动作日。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 全期与再分配日子集是两个逻辑子图。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** 目标跟踪控制通过重分配可分配泄流逼近 TDG 目标；局部流量与 OFF 不可交换。补充：MAE/RMSE/PBIAS 与 NSE/KGE 互补——MAE/RMSE 给绝对误差尺度，PBIAS 给百分比偏差方向，NSE/KGE 给相对技巧与分解；报告应并列而非互相替代。

**能得出什么结论：** 门控技能伴随流量改写；必须记录 control_state。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 不证明气体参数最优；无 OOS。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** 负相关≠DART 坏了。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：为追目标把水挪位置；挪完分数好看，不代表关掉定速也好看。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 图 19　泄流对照散点（QGT / TDGTA vs DART）

![图 19　泄流对照散点（QGT / TDGTA vs DART）](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/w4_spill_scatter.png)

**背景与作用：** 流量 1:1 散点量化再分配扭曲强度，是门控伴随证据。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 系统性低于 1:1=可分配流量被压低；子集比全期更关键。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** 再分配日约 173.8573→39.2308 kcfs。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 全期点云 vs 再分配日子云。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** 全系统质量守恒可成立，但局部结构流量被重分。补充：MAE/RMSE/PBIAS 与 NSE/KGE 互补——MAE/RMSE 给绝对误差尺度，PBIAS 给百分比偏差方向，NSE/KGE 给相对技巧与分解；报告应并列而非互相替代。

**能得出什么结论：** 报告协议须同时记录控制状态与再分配摘要。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 不扩展生态影响。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** 流量 NSE≠TDG 技能。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：很多点掉到 1:1 下方——水被挪走了。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 4.4 数值健康记录与 DLTMAX 扫描（W2）


*表 4　Long Lake 负表面层厚度计数*
| DLTMAX@JDAY30 | DLTINTER=ON 负厚度 | DLTINTER=OFF 负厚度 |
| --- | --- | --- |
| 20 | 5 | 0 |
| 50 | 4 | 0 |
| 100 | 1 | 0 |
| 200 | 5 | 0 |

> **表述约束**  
> 5/4/1/5 仅官方 DLTINTER=ON 结点扫描；不是窗内硬顶。OFF 全 0。不得写成「减小时间步更不稳」普遍定律。H1&lt;0 目前只在 Long Lake 完成运行中出现。


### 图 20　负厚度计数随 DLTMAX 变化

![图 20　负厚度计数随 DLTMAX 变化](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/nhr_dltmax_neg_thickness.png)

**背景与作用：** 负厚度计数是 NHR 核心字段：exit 0 可掩盖回退。Long Lake DLTINTER=ON 下 DLTMAX 20/50/100/200 s 对应 5/4/1/5（既有扫描权威）；OFF 后为 0。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 按 DLTMAX 读柱；对比 INTER ON/OFF；同时看完成状态。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** 计数来自 wrn/日志解析；缺失文件≠零事件。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 每个 DLTMAX 是逻辑子图；ON/OFF 是条件分层。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** H1<0 触发几何/时间步回退；DLTINTER 是插值日程开关，不是扩散系数。补充：MAE/RMSE/PBIAS 与 NSE/KGE 互补——MAE/RMSE 给绝对误差尺度，PBIAS 给百分比偏差方向，NSE/KGE 给相对技巧与分解；报告应并列而非互相替代。

**能得出什么结论：** NHR 应随技能报告；5/4/1/5 非单调，不是时间步定律。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 不可外推所有水库；Columbia 可为 0。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** 禁止「步长越小越不稳」普遍化。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：体检报警次数要跟成绩单一起交，不能只看「正常结束」。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 图 21　层增减与 DLTMIN 提示

![图 21　层增减与 DLTMIN 提示](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/nhr_dltmax_layers_dltmin.png)

**背景与作用：** 加层/减层与 DLTMIN 回退补充「健康不只一项」。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 分组柱 + INTER 状态 + exit_zero_masks_rollback。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** 多项计数并列；官方 100 s 是结点谷底不是窗内硬顶。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 指标×设置矩阵中的行组。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** ZMIN/DLTMIN 为保护阈值；与 TDG 方程无直接等式。补充：MAE/RMSE/PBIAS 与 NSE/KGE 互补——MAE/RMSE 给绝对误差尺度，PBIAS 给百分比偏差方向，NSE/KGE 给相对技巧与分解；报告应并列而非互相替代。

**能得出什么结论：** 伴随记录，不是稳定性排行榜。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 窗内实际 DLT 仍可高于结点。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** add/subtract 次数≠直接错误判决。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：病历里还有别的化验项，不要只盯一项。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 图 22　NHR 扫描热图概览

![图 22　NHR 扫描热图概览](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/nhr_dltmax_heatmap.png)

**背景与作用：** 热图总览多指标×多设置；服务报告协议。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 颜色=计数强度，不是浓度；先找热点再回表。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** 单元格=事件计数；missing 与 0 要区分。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 每一格是逻辑子图。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** 数值健康事件来自离散化与层几何约束。补充：MAE/RMSE/PBIAS 与 NSE/KGE 互补——MAE/RMSE 给绝对误差尺度，PBIAS 给百分比偏差方向，NSE/KGE 给相对技巧与分解；报告应并列而非互相替代。

**能得出什么结论：** 支持 NHR 随技能报告；不支持普适最优步长。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 仅 Long Lake 扫描点集。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** 最浅色格子≠必须采用的时间步。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：化验单总览，不是药方剂量。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 4.5 Columbia SOD 量级（W7）

瞬时湿段（JDAY≥33）n=1081，均值=0.8762，落在 0.5–3.0 带内 968/1081=89.55%；&gt;3.0 的比例=0.0%；&lt;0.5 的比例=10.45%。 Almeida 成岩最优均值约 1.07。 参数来源：DeGray 模板移植——不是 Columbia 现场率定。


### 图 23　Columbia 湿段 SOD 时间序列与 Almeida 参考

![图 23　Columbia 湿段 SOD 时间序列与 Almeida 参考](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/w7_columbia_sod_timeseries.png)

**背景与作用：** Columbia SOD 时序做移植参数量级检查。湿段均值 0.8762 gO₂/m²/d（JDAY≥33，n=1081）。参数来自 DeGray 成岩模板移植。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 看 spin-up 后湿段轨迹；零值干段已排除语境。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** SOD 通量；非观测技能。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 按时间读，可对照直方图分布。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** SOD=沉积物耗氧需求；与 CSOD/NSOD 相关但本图主报总 SOD。补充：MAE/RMSE/PBIAS 与 NSE/KGE 互补——MAE/RMSE 给绝对误差尺度，PBIAS 给百分比偏差方向，NSE/KGE 给相对技巧与分解；报告应并列而非互相替代。

**能得出什么结论：** 量级检查通过≠现场率定。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 禁止水质情景推断升级。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** 落带≠验证成岩模块。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：没离谱到天上去，也不等于本地泥样标定好了。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 图 24　Columbia SOD 直方图

![图 24　Columbia SOD 直方图](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/w7_columbia_sod_histogram.png)

**背景与作用：** 直方图对照 Almeida 扫描带 0.5–3.0。frac_in=0.8955，frac_<0.5=0.1045，frac_>3=0.0。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 看主体与尾巴；勿把已排除的零干段当零耗氧泥。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** 湿段瞬时 SOD 计数。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 带内/带下/带上三个逻辑区。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** 对照带来自独立实验扫描，非普适生态定律。补充：MAE/RMSE/PBIAS 与 NSE/KGE 互补——MAE/RMSE 给绝对误差尺度，PBIAS 给百分比偏差方向，NSE/KGE 给相对技巧与分解；报告应并列而非互相替代。

**能得出什么结论：** 约 89.6% 落带，结论止于量级。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** 非本地最优证明。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** band≠水质标准。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：大多数不离谱，少数偏低，没有爆表——体检不是处方。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


### 4.6 文献 VPR 审计（W5）


*表 5　W5 文献审计摘要（分母 38，除非注明）*
| 项目 | 计数 | 比例/说明 |
| --- | --- | --- |
| 纳入研究 | 38 | 综述表 1 = 38 |
| 全文可读 | 9 | 23.7% |
| VPR 可重建 yes | 2 | 5.3% |
| VPR unknown | 19 | 无全文且摘要不够 |
| 写出输出文件/列名 | 0 | 0% |
| 报 R² | 11 |  |
| 报 R² 不报 NSE | 9 | 占报 R² 者 81.8% |
| 报 KGE | 0 | 0% |
| 表 2 确认 W2↔观测技能 | 1 | 仅 Neto 0.32 |

表 2 的 12 个 R² 中，经核对：**1** 条确认是 W2 输出对观测技能（Neto 2023 的 0.32）；**7** 条确认是其他数学对象（相关分析、负荷情景曲线、水位指标误标等）；**4** 条仍为 unresolved/unknown（不可把 unknown 并入「非技能」）。这比「没写断面」更重：表在比较不同数学对象。


### 4.7 w2eval run-card（W6）


*表 6　run-card 清单*
| card_id | 标题 | 模式 |
| --- | --- | --- |
| bonneville_tdgta_on | Bonneville TDGTA ON — three calibers + SYSTDG pre-control snapshot | skill_vs_observations |
| bonneville_tdgta_off | Bonneville TDGTA OFF — B file gone; S is pre-control snapshot | skill_vs_observations |
| longlake_dlt_nhr | Long Lake official vs DLTMAX scan (DLTINTER ON/OFF) | numerical_health |
| columbia_do_internal | Columbia DO internal consistency + SOD magnitude note | internal_consistency |
| degray_t_internal | DeGray temperature internal consistency (no independent observations) | internal_consistency |


### 图 25　w2eval run-card 概念/示例拼图

![图 25　w2eval run-card 概念/示例拼图](I:/Projects/20260810-CE-QUAL-W2/06_PAPER/figures/fig07_w2eval_runcard.png)

**背景与作用：** run-card 固化 VPR+指标+NHR，防对象漂移；是评估数据结构而非新过程模块。它在报告结构中位于结果章对应小节，上承方法中的 VPR/控制律/NHR 定义，下启讨论中的硬边界复核。

**怎么读（坐标轴 / 曲线 / 散点 / 直方图 / 热图）：** 先读对象与控制状态，再读分数，最后读 NHR。读图时固定顺序：对象定义（VPR）→ 坐标与图例 → 逐曲线/逐子图 → 指标数字（以 JSON 为准）→ 证据层声明（skill / internal / obs-obs / NHR / 量级）。跳过任何一步都容易越界解释。

**每条曲线（或分量）含义：** 卡片字段对应评价自变量；internal NSE 不进 skill 表。每条序列的文件名、列名、segment、layer、单位与时间支撑若 JSON 未给全，对应处标「待补充」，不猜测填空。

**逐子图 / 逐面板精读：** 卡片分区=逻辑子图（VPR / metrics / NHR）。若视觉上为单面板，仍按曲线/分量/口径/条件（ON/OFF、INTER ON/OFF、年份、再分配子集）切成逻辑子图逐一讲解，避免「一张图一句空话」。

**物理意义与方程链：** 评价函数自变量是被抽取序列；卡把抽取规则写死。补充：MAE/RMSE/PBIAS 与 NSE/KGE 互补——MAE/RMSE 给绝对误差尺度，PBIAS 给百分比偏差方向，NSE/KGE 给相对技巧与分解；报告应并列而非互相替代。

**能得出什么结论：** 完整≠模型有效；OFF 有 S 不补 B。能写进结论的句子必须同时满足：对象清楚、控制状态清楚、样本定义清楚、证据层清楚；任一缺失则降级为假设或标「待补充」，不得升格为普遍规律或样本外技能。

**原因、替代解释与证据边界：** MVP 不自动跑 W2。证据边界再次强调：数字只在既有运行、配对窗与 analysis JSON 记录范围内成立；缺失项标「待补充」；禁止把内部一致性写成观测技能，禁止把门控文件缺失写成物理量删除，禁止把 NHR 写成时间步定律，禁止把超标频率写成样本外 NSE，禁止把 SOD 落带写成现场率定，禁止把 W5 unknown 改写成 no。

**常见误读与排除：** 卡片不是验证徽章。常见误读排除清单：相关性当作一致性；内部通道对照当作观测验证；文件存在性直接等同物理过程存在性；OFF 缺文件当作零序列；只摘 R² 不报 NSE/α/β；把 exit 0 当作数值清白；把 run-card 完整当作模型已验证。

**通俗版结论：** 通俗复述：先写卷头考哪一科、开没开定速、有没有报警，再看分数。再用一句大白话收束：先问清楚「比的是哪份文件、哪一站、哪一层、控制器开没开、有没有数值报警」，再看分数；分数再好看，越界解释也无效。


## 分析与讨论

四条证据链应当合读，而不是拆成互不相关的「四个创新点口号」。变量误指认（W1）解释为什么跨论文 R² 需要 VPR；控制律门控（W3/W4）解释为什么同一名称的 TDG 仍可能不可比；NHR（W2）解释为什么 exit 0 不是数值清白证明；文献审计（W5）把前三者从个案升为报告规范动机。SOD（W7）则提醒：可复现运行起来 ≠ 现场过程已率定。

- 条件可比，而非绝对不可比：当且仅当 VPR、控制律、NHR、证据类型对齐时，技能数字才可并置。
- NHR 是报告建议（should），不是强制标准警察；本报告也未验证全部 17 例框架。
- W5 的 confirmed vs unknown 必须分开写：unknown 不是 no。
- 2016–2025 超标频率可讨论可达范围，不可冒充样本外预报技能。


### 为何 R²–NSE 分离首先是对象定义问题

把 R²–NSE 分离仅解释成“指标偏好不同”仍不够深。更根本的原因是：输出通道常内含不同空间积分算子。表层值、库容平均、取水流量加权值、某高程结构出流、另一断面的表层值，虽然单位相同、变量名相近，对应的状态向量投影并不同。季节或潮汐共同强迫可使这些投影保持高相关，而积分范围、均值水平与方差响应又使它们远离 1:1。α 与 β 因而不是抽象统计补丁，而是空间算子错配在统计空间中的指纹。


### 为何门控技能不能外推为无条件技能

ON-B 的 NSE 为正并不虚假，但它回答的是一个条件问题：在 TDGTA 开启、泄流被控制器重分配、后控制目标文件存在且值域受目标带约束时，B 与 CCIW 的配对表现如何。它不能回答原泄流计划、控制器关闭或2016–2025 边界条件下的无条件预报能力。S 文件 ON/OFF 完全一致又证明，控制前状态与后控制目标是两种对象。因此正确做法不是否定 B 的数字，而是把控制状态写入数字的下标与 VPR。


### 为何 NHR 是伴随证据而非时间步定律

Long Lake 的 5/4/1/5 说明在这套日程、这套水位轨迹和 DLTINTER=ON 条件下，结点值与负厚度次数呈非单调关系。它没有识别一个普遍的 CFL 稳定性函数，也没有证明更小局部 Δt 导致不稳定。事实上 DLTINTER=ON 时结点之间线性插值，JDAY30 的 20 s 不是整个窗的硬上限；OFF 后四组负厚度均为 0，则进一步表明路径构造方式是关键条件。NHR 的科学价值在于让这种条件被看见，而不是把八次运行升级成普遍数值定律。


### 阅读本报告图件时的统一规程

对本报告纳入的每一张图，正文固定采用九段式讲解：背景与全篇作用、怎么读、每条曲线/分量含义、逐子图/逐面板精读、物理意义与方程链、结论、原因与替代解释及证据边界、常见误读与排除、通俗复述。九段式不是修辞装饰，而是强制把图形从「插图点缀」升级为可审计证据：读者应能不打开 JSON 也能知道该图回答什么问题、不回答什么问题。若某图缺少文件或指标，统一标「待补充」，禁止补造曲线。

硬边界在图解层同样生效：internal consistency 图不得写入观测技能表；门控文件缺失不得写成物理量删除；NHR 计数不得写成时间步定律；超标频率不得写成 OOS NSE；SOD 落带不得写成现场率定；W5 unknown 不得改写成 no。


### 证据链的可复用形式

本研究可复用的不是某个阈值，而是四步顺序：先用 VPR 锁定数学对象，再声明控制律，随后计算多指标并分解 α/β，最后附上 NHR 与证据类型。只要顺序颠倒，读者就可能先看到一个漂亮分数，再被迫猜测分数究竟来自哪一站、哪一层、哪个文件和哪个控制状态。run-card 将这四步固定在同一记录中，其贡献属于报告协议，不属于 CE-QUAL-W2 新过程方程。


## 主要结论

- Bonneville（观测技能，n=1614）：同一 CCIW 上 R² 落在约 0.5082–0.5512 的窄带，而 NSE 为约 −2.8044 / +0.5 / −2.7516；技能最好且 β≈1、峰值钉在约 120.1% 的序列只存在于 TDGTarget 门控文件；SYSTDG 控制前快照仍写入 TDG_output 且 ON≡OFF，故不是「物理量删除」。
- DeGray / Columbia（内部一致性）：T2 vs Tvolavg 给出 R²≈0.9027 而 NSE≈−0.5855；DO I49 vs I33 给出 R²≈0.6505 而 NSE≈−1.4821。官方案例无独立观测时，禁止把这些数字写成对观测技能。
- NHR：exit 0 可与负厚度回退并存；Long Lake DLTINTER=ON 下 DLTMAX 20/50/100/200 s 负厚度 5/4/1/5（非单调），OFF 后全 0；因此 NHR 应随技能报告，而不是被表述为普遍时间步定律。
- DART 核对：小时 n=17805，MAE=0.026537，匹配率≈0.994945，支持示例观测未被实质性改写；2016–2025 超标小时 21.2% 只是观测描述，无样本外 NSE。
- W5：可重建 VPR 2/38；表 2 确认 W2–观测技能 1/12；文献 KGE 报出数为 0；全文 9/38，unknown=19。
- Columbia SOD：湿段均值 0.8762 gO₂/m²/d，约 89.55% 落在 0.5–3.0 对照带，但是 DeGray 成岩模板移植，非现场率定。
- 可复用产物是 VPR→控制律→多指标+α/β→NHR→证据类型 的报告协议（及 w2eval run-card），不是新的 CE-QUAL-W2 过程方程。


## 不足与展望

- 样本外 NSE：待补充（需 TMEND 出 2011，并准备 2016+ 气象/边界/出流）。
- DeGray / Columbia 独立实测：待补充。
- 负厚度非单调：目前 n=1 水域（Long Lake），不可外推到所有 W2 应用。
- W5 全文获取 9/38，unknown=19：待补充合法全文后可下修不确定。
- Zenodo DOI：待补充。
- 本报告未重跑完整 W2；图为既有 JSON/CSV 的 SciencePlots 重绘。


## 附录：文件清单与复算入口


### A. 关键 JSON

- `06_PAPER/analysis/w1_provenance_metrics.json`
- `06_PAPER/analysis/w3_tdgta_off_metrics.json`
- `06_PAPER/analysis/w4_cciw_vs_dart.json`
- `06_PAPER/analysis/w5_lit_audit_summary.json`
- `06_PAPER/analysis/w7_columbia_sod_vs_almeida.json`
- `06_PAPER/analysis/nhr_dlt_scan.json`
- `06_PAPER/analysis/nhr_existing_runs.json`


### B. 复算入口（命令级）

以下命令用于复算分析产物；完整模型重跑不在本报告范围。

```text
python 06_PAPER/analysis/w1_w7_provenance.py
python 00_INDEX/eval_w3_tdgta_off.py
python 00_INDEX/download_dart_cciw.py  # 或 --skip-download 仅分析
python 00_INDEX/parse_nhr.py --existing --out 06_PAPER/analysis/nhr_existing_runs.json
python 06_PAPER/analysis/plot_nhr_scan.py
python 06_PAPER/analysis/build_research_report.py
```


### C. 本报告标明的「待补充」汇总

- 样本外 NSE（需扩展 TMEND 与 2016+ 边界后计算）
- Columbia / DeGray 独立野外观测（目前无，不得假装有 skill）
- W5 其余 29 篇全文（付费墙）；VPR unknown=19 维持 unknown，不猜测
- Zenodo DOI（清单已在 06_PAPER/zenodo/，尚未上传铸造）
- T3 跨版本漂移实验
- T4 NHR 源码插桩（TSR 抽样无法代表 DLTMIN 时长占比）


### D. run-card 路径

- `06_PAPER/w2eval/cards/bonneville_tdgta_on.json` — Bonneville TDGTA ON — three calibers + SYSTDG pre-control snapshot
- `06_PAPER/w2eval/cards/bonneville_tdgta_off.json` — Bonneville TDGTA OFF — B file gone; S is pre-control snapshot
- `06_PAPER/w2eval/cards/longlake_dlt_nhr.json` — Long Lake official vs DLTMAX scan (DLTINTER ON/OFF)
- `06_PAPER/w2eval/cards/columbia_do_internal.json` — Columbia DO internal consistency + SOD magnitude note
- `06_PAPER/w2eval/cards/degray_t_internal.json` — DeGray temperature internal consistency (no independent observations)


---
内嵌/引用图数量（HTML Base64）：25
